# ToonCar Track Viewer

Boilerplate aplikacji React + Vite + Tailwind + Three.js do prezentacji animowanych tras z gry ToonCar.

## Uruchomienie

```bash
pnpm install
pnpm dev
```

Nową trasę dodaj w `src/tracks.ts`, a pliki GLB i equirektangularny skybox umieść w `public/tracks/<id>/`.
